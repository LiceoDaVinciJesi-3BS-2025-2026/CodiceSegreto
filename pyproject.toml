[project]
name = "codicesegreto"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "platformdirs>=4.9.4",
    "pygame>=2.6.1",
]

[project.scripts]
codicesegreto = "codicesegreto:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
